# Daily Report - Garnish - 2026-04-06

## Summary
Massive day. Designed and architected the entire project from scratch (10-phase plan), then shipped Phases 1, 2, and 3 — going from empty repo to a working multi-tenant recipe app with authentication, household management, and full recipe CRUD/browse/edit. Also paused mid-stream for an auth security audit and refactor.

## Progress Today
- **Project planning**: Defined vision, scope, tech stack, and 10-phase roadmap. Created `docs/plan/plan.md` and individual phase files.
- **Phase 1: Foundation** — Rails 8 API + React 19 PWA monorepo, JWT auth (access + refresh via httpOnly cookie), Tailwind CSS 4, TanStack Query, React Router 7, Docker Compose for Postgres, GitHub Actions CI/CD with Tailscale SSH deploy
- **Phase 2: Households** — Multi-tenancy (Household, HouseholdMembership), invite codes (food-themed: SAUTE-SAGE-84), roles (owner/admin/member), grocery permissions (read/contribute/full), HouseholdScoped concern, onboarding flow, member management UI
- **Auth/session audit + refactor** — Ran project-auditor agent (verdict: B-, sound architecture). Added `apikey` column to User exposed as `id` in API, refactored refresh tokens to single `<apikey>.<random>` cookie, JWT type validation, fixed Safari logout bug, fixed HouseholdContext race condition, added 17 minitest auth specs
- **Phase 3: Recipes Core** — Recipe model with JSONB ingredients/instructions, RecipePolicy with nested Scope class, RecipesController with full CRUD/search/filter, 20 backend tests. Frontend: RecipeBrowser with smart filter chips (only show types/categories that exist), RecipeCard, RecipeDetail, create/edit forms with IngredientEditor and InstructionEditor (sections, reordering, optional timers), single-recipe JSON export
- **Polish**: Quick meals can now have optional servings (for leftover tracking)

## Decisions Made
Five architectural decisions logged in DECISIONS.md (all today):
1. Rails API + React PWA monorepo
2. Self-hosted on MacBook Pro via Cloudflare Tunnel (no Docker)
3. Recipes belong to households, with export-on-leave
4. LLM features as optional BYOK enhancement
5. Meal plan entries as polymorphic types (recipe / quick_meal / event / note)

## Blockers & Issues
None

## Next Steps
**Phase 4: Recipe Ingestion** — URL/PDF/image import with optional LLM enhancement
- Set up ActiveStorage + Cloudflare R2 (also enables image uploads)
- URL import via JSON-LD/Schema.org parsing (free, no LLM)
- PDF/image import with sage-rb LLM extraction (user-provided keys)
- Background job processing with status updates
- Encrypted user API key storage
- Fallback flow: store source material as attachment, manual entry

## Context for Next Session
- **Image upload**: Not yet implemented but `image_url` field exists. Should be tackled as part of Phase 4 since ActiveStorage will be set up anyway. Adding `has_one_attached :photo` is trivial once R2 is wired up.
- **Uncommitted changes**: All Phase 3 work is unstaged (recipe model, controller, policies, frontend pages, tests). Need to commit before starting Phase 4.
- **Auth gotchas to remember**:
  - Don't use `as: :json` on GET requests in tests (it wraps params under the resource key) — use query strings instead
  - Test-database transactional cleanup needs attention if any tests modify users across requests
- **Architecture pattern established**: New models follow apikey-as-id pattern. New controllers using `policy_scope(Model)` automatically get household-scoped queries with role-based filtering.
- **JWT_SECRET in `application.yml`** is committed gitignored — don't lose it or all sessions invalidate.
