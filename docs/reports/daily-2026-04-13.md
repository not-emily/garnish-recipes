# Daily Report - Garnish - 2026-04-13

## Summary
Short but focused session knocking out polish items from the backlog. Shipped three quick wins (default recipe sort, mobile input zoom fix, image aspect ratio bug) and expanded the next-session backlog with a handful of new ideas. No new features, all UX polish and bug fixes.

## Progress Today
- Changed default recipe sort from "Last Updated" to "Recently Cooked" — shows actively-rotated recipes first, with alphabetical fallback for never-cooked ones
- "Recently Cooked" now appears as a quiet default (no filter pill, no count), and moved to the top of the Sort By options
- Fixed missing backend "rating" sort support (was silently falling through to updated_at)
- Added global `font-size: 16px` on inputs/textareas/selects to prevent iOS Safari auto-zoom on focus
- Fixed recipe card image aspect ratio on production Chromium — switched from `aspect-ratio: 4/3` to the `padding-bottom: 75%` trick because Chromium was ignoring the aspect-ratio property and sizing the container to each image's intrinsic dimensions
- Expanded next-session backlog: recipe images, cooking mode, quick filter pills, fraction support, "what's on the menu" banner, instruction sections

## Decisions Made
None (no new architectural decisions — all polish work)

## Blockers & Issues
- **Mobile cross-week swipe**: Still unresolved from previous session
- **Imported recipe ingredient quality**: Still unresolved from previous session

## Next Steps
Top of the backlog for next session:
1. Recipe images (add/edit)
2. Cooking mode (toggle between recipes in a meal slot)
3. Quick filter pills on recipe browse
4. Fraction support for ingredient quantities
5. "What's on the menu" banner

## Context for Next Session
- **Chromium aspect-ratio quirk**: `aspect-ratio: 4/3` on a flex-column child was being overridden by the intrinsic size of an `<img>` descendant, even with `object-cover`. The container rendered at the image's natural ratio (2:3, 3:4, etc.) instead of 4:3. Only reproducible in the production build — local dev rendered correctly. Resolved with `padding-bottom: 75%` trick + absolutely-positioned image. Worth remembering if similar aspect-ratio bugs appear elsewhere.
- **DB backup cron**: Emily set up a cron job on the MacBook to run `scripts/backup-db.sh` daily. She'll verify tomorrow. If it fails silently, first thing to check is the `pg_dump` path (cron has a minimal PATH on macOS).
- **R2 migration**: `storage.yml` already has the Cloudflare R2 service defined. When ready to migrate off local disk storage, just set env vars, flip `config.active_storage.service` to `:cloudflare`, and run `active_storage:mirror` to copy existing files.
- Build broke once today on a TypeScript strict error — the `"rating"` sort is client-side only and not a valid value for the API `RecipeFilters.sort` type. Remember this constraint when touching sort logic.
