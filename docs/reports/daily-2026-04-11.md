# Daily Report - Garnish - 2026-04-11

## Summary
Massive productivity day: shipped Phase 5D (ActionCable real-time sync smoke test), the entirety of Phase 6 (leftover calculation, tray, cascade delete), and the entirety of Phase 7 (grocery lists with generation, categorization, store tagging, permissions, and real-time sync). Three phases worth of work in one session, including multiple rounds of smoke testing and bug fixes.

## Progress Today
- Smoke-tested Phase 5D ActionCable real-time meal plan sync (all four broadcast types verified end-to-end)
- Built and shipped Phase 6: Leftovers (sub-phases A+B)
  - LeftoverCalculator service, LeftoverPrompt with toggle + slot pickers, LeftoverEntry visual variant
  - LeftoverTrayItem model, LeftoversController, leftover tray UI with schedule popover, cascade delete with confirmation dialog
  - Household settings: leftover_expiry_days, leftover_default_slot
  - Smoke test fixes: cross-week leftover creation, tray item merging, schedule popover dates, note double-submit, event notes icon, recipe edit image_url fix
- Built and shipped Phase 7: Grocery Lists (sub-phases A+B + refactor)
  - GroceryGenerator service with ingredient scaling, keyword categorization (word-boundary + compound keywords), household mapping overrides, store auto-tagging, aggregation/dedup
  - GroceryListsController with CRUD, generate, check/uncheck, store management, permissions
  - Refactored from week-scoped lists to single rolling list per household with date-range generation
  - Frontend: category-grouped sections with emoji headers, row-tap-to-edit, auto-categorize + auto-store on manual add, store pill filter with inline add/manage, permission-gated UI, clear checked, real-time sync
  - excluded_items tracking so deleted items don't return on regeneration
  - 191/191 backend tests, TypeScript + vite build clean

## Decisions Made
- Leftovers: separate LeftoverTrayItem model (not nullable date/slot on MealPlanEntry) for cleaner separation
- Leftovers: tray items merged into one per source entry (not N separate items)
- Grocery: single rolling list per household (not week-isolated) with day-granularity date range generation
- Grocery: ingredient_category_mappings table for learned category + store corrections that persist across regenerations
- Grocery: fixed category list (18 categories) rather than open-ended user-defined categories
- Grocery: store tags as household-level store list with auto-tag via mappings
- Grocery: excluded_items JSONB to prevent deleted items from returning on regeneration

## Blockers & Issues
- **Mobile cross-week swipe**: Framer Motion drag + DndContext interaction at week boundaries. Not blocking — desktop week nav works.
- **Imported recipe ingredient quality**: Phase 4 ingestion stores full text in `name` field. Breaks grocery aggregation/dedup for imported recipes. Needs ingestion pipeline fix.

## Next Steps
1. Phase 8 or next priority from the plan
2. Debug mobile cross-week swipe bug
3. Fix imported recipe ingredient parsing (Phase 4 ingestion quality)

## Context for Next Session
- 191 backend tests passing, TypeScript + vite build clean
- Phase 7 has uncommitted changes (Phase 6 was committed as `8e41462`, Phase 7 is staged but not committed yet)
- The grocery list ActionCable sync uses invalidate-on-broadcast with a 15s poll fallback — the broadcast path may have intermittent issues in incognito/multi-tab but the poll catches it
- Category heuristic uses word-boundary matching + compound keyword priority — "chopped" no longer matches "chop", "chicken broth" correctly maps to canned_jarred
- The ingredient parsing issue from Phase 4 means imported recipes have full-text ingredient names (e.g. "2 lbs beef") that can't be aggregated. This is a separate fix from Phase 7.
